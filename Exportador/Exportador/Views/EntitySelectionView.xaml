<UserControl x:Class="Exportador.UI.Views.EntitySelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:behaviors="clr-namespace:Exportador.UI.Behaviors"
             xmlns:services="clr-namespace:Exportador.UI.Services"
             xmlns:viewmodels="clr-namespace:Exportador.UI.ViewModels"
             mc:Ignorable="d"
             Background="#F5F7FA" d:DesignWidth="880"
             d:DataContext="{d:DesignInstance Type=viewmodels:EntitySelectionViewModel}">

    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0 0 0 13" Width="700">
            <TextBlock Text="Passo 2 de 5: Seleção de Entidades"
                       FontSize="16" FontWeight="SemiBold" Foreground="#7F8C8D" Margin="0 0 0 20"/>

            <StackPanel Orientation="Vertical" HorizontalAlignment="Center" Margin="0 0 0 15">
                <Canvas Width="60" Height="60" HorizontalAlignment="Center">
                    <Ellipse Width="60" Height="60" Stroke="#3498DB" StrokeThickness="1" Canvas.Left="0" Canvas.Top="0" />
                    <Image Source="/Images/entidade.png" Width="42" Height="42" Canvas.Left="9" Canvas.Top="9"  RenderOptions.BitmapScalingMode="HighQuality" />
                </Canvas>
                <TextBlock Text="Seleção de Entidades"
                           FontSize="28"
                           FontWeight="Bold"
                           Foreground="#34495E"
                           Margin="0 20 0 10"
                           HorizontalAlignment="Center"/>
                <TextBlock Text="Escolha quais categorias de dados deseja exportar"
                           FontSize="16"
                           Foreground="#7F8C8D"
                           TextWrapping="Wrap"
                           TextAlignment="Center"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Width="700">
                <TextBlock Text="{Binding SelectedEntitiesCountMessage}"
                           FontSize="16" Foreground="#7F8C8D" TextWrapping="Wrap" TextAlignment="Left"
                           Width="470" Margin="0 0 0 10"/>

                <Button Content="Selecionar Tudo" Width="110" Height="24" Style="{StaticResource NavigationButtonStyle}" Margin="0 0 0 20">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="Click">
                            <i:InvokeCommandAction Command="{Binding SelectAllCommand}" />
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </Button>
                <Button Content="Limpar Seleção" Width="110" Height="24" Style="{StaticResource NavigationButtonStyle}" Margin="10 0 0 20">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="Click">
                            <i:InvokeCommandAction Command="{Binding ClearSelectionCommand}" />
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </Button>
            </StackPanel>

            <ScrollViewer Height="400" VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding AvailableEntities}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="White" CornerRadius="8" Padding="20" Margin="0 0 0 10">
                                <Border.Effect>
                                    <DropShadowEffect ShadowDepth="2" Direction="270" Color="#CCCCCC" Opacity="0.5" BlurRadius="5"/>
                                </Border.Effect>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Top">
                                    <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                              Margin="5,0,0,0" IsThreeState="False" Width="25"
                                              HorizontalAlignment="Left" VerticalAlignment="Center" />
                                    <Image Width="32" Height="32" Source="{Binding ImagePath}" Stretch="Uniform" VerticalAlignment="Top" HorizontalAlignment="Center" />
                                    <TextBlock Text="{Binding Name}" FontSize="16" Foreground="Black" TextWrapping="Wrap" FontWeight="Bold"
                                               TextAlignment="Center" Padding="10 2 2 2" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </StackPanel>
    </Grid>
</UserControl>