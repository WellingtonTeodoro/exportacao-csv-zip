<UserControl x:Class="Exportador.UI.Views.ResultView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:behaviors="clr-namespace:Exportador.UI.Behaviors"
             xmlns:services="clr-namespace:Exportador.UI.Services"
             mc:Ignorable="d"
             Background="#F5F7FA" d:DesignWidth="880">

    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0 30 0 13" Width="700">
            <TextBlock Text="Passo 5 de 5: Resultado"
                       FontSize="16" FontWeight="SemiBold" Foreground="#7F8C8D" Margin="0 0 0 20"/>

            <StackPanel Orientation="Vertical" HorizontalAlignment="Center" Margin="0 0 0 30">
                <Canvas Width="60" Height="60" HorizontalAlignment="Center">
                    <Ellipse Width="60" Height="60" Stroke="#3498DB" StrokeThickness="1" Canvas.Left="0" Canvas.Top="0" />
                    <Image Source="/Images/resultado.png" Width="42" Height="42" Canvas.Left="9" Canvas.Top="9"  RenderOptions.BitmapScalingMode="HighQuality" />
                </Canvas>
                <TextBlock Text="Exportação Concluída!"
                           FontSize="28"
                           FontWeight="Bold"
                           Foreground="#34495E"
                           Margin="0 20 0 10"
                           HorizontalAlignment="Center"/>
                <TextBlock Text="Os dados foram exportados com sucesso"
                           FontSize="16"
                           Foreground="#7F8C8D"
                           TextWrapping="Wrap"
                           TextAlignment="Center"/>
            </StackPanel>

            <StackPanel Margin="10"> 
                <Border Background="#F5F5F5" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="5" Padding="10" Margin="10">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Text="Resumo da Exportação" Foreground="#00A859" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,5"/>

                        <TextBlock Grid.Row="1" Text="Detalhes do arquivo gerado" Foreground="#333333" FontSize="14" Margin="0,0,0,5"/>

                        <Grid Grid.Row="2" Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Arquivo:" FontWeight="Bold" Margin="0,0,5,5" Foreground="#333333"/>
                            <TextBlock Grid.Row="0" Grid.Column="1" 
           Text="{Binding ZipFileName}" 
           Margin="0,0,20,5" Foreground="#333333"/>

                            <TextBlock Grid.Row="0" Grid.Column="2" Text="Tamanho:" FontWeight="Bold" Margin="0,0,5,5" Foreground="#333333"/>
                            <TextBlock Grid.Row="0" Grid.Column="3" 
           Text="{Binding ZipFileSizeDisplay}" 
           Margin="0,0,0,5" Foreground="#333333"/>

                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Registros:" FontWeight="Bold" Margin="0,0,5,5" Foreground="#333333"/>
                            <TextBlock Grid.Row="1" Grid.Column="1" 
           Text="{Binding TotalRecords, StringFormat={}{0:N0} registros exportados}" 
           Margin="0,0,20,5" Foreground="#333333"/>

                            <TextBlock Grid.Row="1" Grid.Column="2" Text="Tempo:" FontWeight="Bold" Margin="0,0,5,5" Foreground="#333333"/>
                            <TextBlock Grid.Row="1" Grid.Column="3" 
           Text="{Binding TotalExportTime}" 
           Margin="0,0,0,5" Foreground="#333333"/>

                        </Grid>

                        <TextBlock Grid.Row="4" Text="Categorias Exportadas:" Foreground="#333333" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10"/>

                        <StackPanel Grid.Row="5" Orientation="Horizontal" Margin="0,0,0,10">
                            <ItemsControl Grid.Row="5" ItemsSource="{Binding ExportedTables}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,20"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="#CCF0E0" CornerRadius="3" Padding="8,4" Margin="0,0,10,0">
                                            <TextBlock Text="{Binding TableName}" Foreground="#007F3F" FontSize="12"                   FontWeight="SemiBold"/>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>

                        </StackPanel>

                        <TextBlock Grid.Row="7" Text="Arquivos Incluídos:" Foreground="#333333" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,0"/>

                        <StackPanel Grid.Row="8">
                            <ItemsControl Grid.Row="8" ItemsSource="{Binding ExportedTables}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Margin="0,0,0,3" Foreground="#333333" FontSize="13">
                                            <Run Text="• "/>
                                            <Run Text="{Binding TableName}"/>
                                            <Run Text=".csv ("/>
                                            <Run Text="{Binding SqlRecordsCount, StringFormat={}{0:N0}}"/>  
                                            <Run Text=" registros)"/>
                                        </TextBlock>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>

                        </StackPanel>

                    </Grid>
                </Border>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>